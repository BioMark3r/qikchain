{
  "name": "qikchain",
  "genesis": {
    "gasLimit": "{{BLOCK_GAS_LIMIT}}",
    "difficulty": "{{GENESIS_DIFFICULTY}}",
    "baseFeeEnabled": "{{BASE_FEE_ENABLED}}",
    "extraData": "{{VALIDATOR_EXTRA_DATA}}",
    "alloc": "{{PREALLOCATIONS}}"
  },
  "params": {
    "chainID": "{{CHAIN_ID}}",
    "minGasPrice": "{{MIN_GAS_PRICE}}",
    "engine": {}
  },
  "bootnodes": []
}
