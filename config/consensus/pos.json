{
  "consensusMode": "ibft-pos",
  "consensus": {
    "type": "ibft",
    "ibft": {
      "validatorType": "ecdsa",
      "mode": "stake",
      "epochSize": 100,
      "blockTime": "2s",
      "staking": {
        "enabled": true,
        "contract": "{{STAKING_ADDRESS}}"
      },
      "validatorSetContract": "{{VALIDATOR_SET_ADDRESS}}",
      "blockRewards": {
        "enabled": true,
        "rewardPerBlock": "{{POS_BLOCK_REWARD}}"
      }
    }
  },
  "pos": {
    "validatorSetContract": "{{VALIDATOR_SET_ADDRESS}}",
    "stakingContract": "{{STAKING_ADDRESS}}"
  }
}
